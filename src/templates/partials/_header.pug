header.circle--header
    .bounds
        .circle--fluid
            .circle--fluid--cell
                a.logo(href='index.html')
                    include ./_logo
                    h1 @#{username}
            .circle--fluid--cell.align-right
                nav
                    ul.circle--inline
                        li
                            a 
                                | Sign Out